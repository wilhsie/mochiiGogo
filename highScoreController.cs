using UnityEngine;
using System.Collections;

public class highScoreController : MonoBehaviour {
	// Update is called once per frame
	void Update () {
	
	}

	// Everytime there is an update to the database, we want to grab the top ten high scores.

	// Steps:
	// Connect to database
	// server says "database has been updated"
	// client requests high scores
	// display high scores
}
